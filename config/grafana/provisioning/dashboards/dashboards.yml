apiVersion: 1

providers:
  - name: "default"
    orgId: 1
    folder: "" # 루트 폴더에 대시보드를 생성
    folderUid: ""
    type: file
    disableSecurityAttribute: false
    options:
      path: /etc/grafana/provisioning/dashboards
      foldersFromFilesStructure: true # 폴더 구조를 파일 구조에서 가져오도록 설정합니다.
